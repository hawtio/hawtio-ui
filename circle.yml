machine:
  node:
    version: 0.10.33
  services:

dependencies:
  pre:
    - npm install -g bower gulp
    - npm install
    - bower install
  override:
    - gulp build

test:
  override:
    - echo yes

deployment:
  website:
    branch: master
    owner: hawtio
    commands:
      - gulp build site deploy


