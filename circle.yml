machine:
  node:
    version: 0.10.33
  services:

dependencies:
  pre:
    - npm install -g bower gulp
    - npm install
    - bower install
  override:
    - gulp build site deploy

test:
  override:
    - echo yes



